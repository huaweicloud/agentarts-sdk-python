"""Pytest configuration for the agentarts memory installer tests.

The installer lives in ``agentarts.toolkit.plugins.memory.installer`` and is
importable once the SDK is installed (editable).  No sys.path hacks needed.
"""
