"""Pytest configuration for the agentarts memory code_agent tests.

The adapter server lives in ``agentarts.toolkit.plugins.memory.server``
and is importable once the SDK is installed (editable).  No sys.path hacks needed.
"""
