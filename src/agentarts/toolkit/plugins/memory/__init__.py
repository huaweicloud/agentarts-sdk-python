"""AgentArts memory plugins package.

Sub-packages: ``ai_agent`` (platform-specific plugin assets), ``mcp``
(MCP server), ``resources`` (manifests + shared hook scripts),
``installer`` (unified installer CLI).
"""
